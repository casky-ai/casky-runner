# ghcr.io/casky-ai/skills/network
# apt — all in /usr/bin
tshark
tcpdump
nmap
suricata
masscan
tcpflow
# zeek installs to /opt/zeek/bin/ (not in PATH) — verified via /opt/zeek/bin/zeek
# runtime
python3
